// Frontend-scoped PostCSS config (CommonJS)
// Minimal on purpose: avoids requiring Tailwind when not needed in tests/build.
// If you add Tailwind, install 'tailwindcss' and 'autoprefixer' and uncomment below.

module.exports = {
  plugins: {
    // tailwindcss: {},
    // autoprefixer: {},
  },
};
