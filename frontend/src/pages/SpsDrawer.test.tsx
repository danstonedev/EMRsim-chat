import { describe, it } from 'vitest'

// Legacy SpsDrawer UI has been retired; keep a placeholder suite so Vitest remains satisfied.
describe.skip('SpsDrawer legacy placeholder', () => {
	it('is intentionally empty', () => {
		// no-op
	})
})
